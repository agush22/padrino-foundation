<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:images, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:images, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :image, url(:images, :create), :class => 'form-horizontal', :multipart => true do |f| %>
    <%= partial 'images/form', :locals => { :f => f } %>
 <% end %>
</div>
